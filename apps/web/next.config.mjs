// Bewusst schlank: der Compiler ersetzt `next build` vollstaendig und liest
// die App an Ort und Stelle. Was hier steht, gilt fuer `next dev`/`next build`
// auf der Entwicklermaschine.
/** @type {import('next').NextConfig} */
export default {
  reactStrictMode: true,
};
