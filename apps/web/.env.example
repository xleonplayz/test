# Die EINE Adresse, die die Plattform kennt. Nur auf dem Server gelesen
# (Server Components, Route Handlers) — keine NEXT_PUBLIC_-Variable.
# Vorgabe ist der compose-Name; auf der Cap traegt man die vergebene
# Adresse von api ein (https://<slug>.dev.lilylabs.app).
API_URL=http://api:8081
