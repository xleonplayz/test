{
  "name": "xleonplayz-monorepo",
  "private": true,
  "version": "0.2.0",
  "description": "Vier Dienste in vier Sprachen, die miteinander reden — ein Pruefstand fuer Erkennung, Bau und Rollout.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "smoke": "bash tools/smoke.sh"
  }
}
