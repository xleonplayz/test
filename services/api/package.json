{
  "name": "@xleonplayz/api",
  "version": "0.2.0",
  "private": true,
  "description": "Das Bindeglied: die einzige Adresse, die die Oberflaeche kennt. Fragt inventory und pricing und legt die Antworten zusammen.",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts --serve",
    "start": "tsx src/server.ts --serve",
    "once": "tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
